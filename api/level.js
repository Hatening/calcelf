const {getUser,admin}=require('../lib/supabase');const {lifetime,monthly}=require('../lib/levels');
module.exports=async(req,res)=>{try{const u=await getUser(req);if(!u)return res.status(401).json({error:'Sign in required'});const {data:p,error}=await admin().from('profiles').select('lifetime_correct,active_days,monthly_solved,monthly_correct,monthly_streak,lifetime_level,monthly_level,plan').eq('id',u.id).single();if(error)throw error;const lt=lifetime(p.lifetime_correct||0),mt=monthly(p.monthly_solved||0,(p.monthly_solved?p.monthly_correct/p.monthly_solved:0),p.monthly_streak||0);res.status(200).json({lifetime:{level:lt},monthly:{level:mt},plan:p.plan})}catch(e){res.status(500).json({error:e.message})}};
